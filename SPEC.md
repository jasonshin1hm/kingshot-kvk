# 킹샷 이벤트 과금효율 계산기 — 구현 명세 (SPEC)

> Claude Code 세션용 문서. 이 리포(kingshot-kvk)에는 두 개의 독립된 앱이 있다.
> 이 SPEC은 **계산기 쪽 작업만** 다룬다. 아래 "건드리지 말 것" 목록을 반드시 지킬 것.

## 0. 리포 구성과 작업 범위

| 파일 | 소속 | 이번 작업 |
|---|---|---|
| `index.html` | 계산기 (공개) | 수정 대상 |
| `items.js` | 계산기 — 아이템 마스터 | 수정 대상 (기본 단위수량 추가) |
| `event-2026-08.js` | 계산기 — 이벤트 데이터 | 참조 (구조 변경 금지) |
| `icons/*.png` | 계산기 — 아이템 아이콘 15종 | 참조 |
| `admin.html` (신규) | 계산기 — 관리자 페이지 | **이번에 만들 것** |
| `kvk.html`, `data.js`, `t4.mjs`, `logo.png`, `assets/` | KvK 통계 앱 | **절대 건드리지 말 것** |

- 계산기는 순수 정적 사이트(빌드 도구 없음, 프레임워크 없음, 단일 파일 지향)를 유지한다.
- 배포: GitHub Pages(리포 전체)와 Netlify(계산기용) 이중 서빙. Netlify는 리포 루트를 그대로 publish.

## 1. 절대 바꾸면 안 되는 도메인 규칙 (이미 실제 데이터로 검증됨)

이 규칙들은 실유저(운영자 Jason)의 수기 계산과 대조해 검증된 것이다. "버그처럼 보여도" 고치지 말 것.

1. **패키지의 모험코인은 작은 숫자다.** 게임 패키지 화면에서 구매 버튼 옆 큰 숫자(500/1,000/2,500/5,000/10,000/25,000/50,000)는 코인이 **아니다**. 실제 코인·조개는 20/40/80/120/200/500/1,000 (p1~p7). `event-2026-08.js`에 이미 반영되어 있음.
2. **코인당 가격은 싼 팩이 가장 효율적**: p1·p2 = 75원, p3 = 93.75원, p4 = 125원, p5 = 150원, p6 = 158원, p7 = 149원. 최적 구성은 "싼 팩부터 일일한도×일수까지 채우기"가 기본.
3. **조개(상점포인트)는 경험치형 재화**. 조개 누적량이 구간(50/200/500/1,000/2,000/3,000/5,000)에 도달하면 코인(25/75/150/200/300/400/600)을 **구간별 1회** 지급. 코인을 소모해도 조개는 쌓이지 않는다.
4. 조개 획득처: 패키지(코인과 동수) + 무료팩(5/일) + 일일특훈(20/일) + 여정 보상(총 25).
5. 무료 코인: 무료팩 5/일 + 일일특훈 20/일(로그인10·임의충전5·야수5 — 에픽모집은 코인 없음) + 여정 최대달성 20.
6. **보상2배 패키지(7,500원)**: 일일특훈 **코인만** 2배. 코인당 75원이므로 정상적인 구매 선택지다.
7. 결과 3안: A = 목표 충족 최저가 / B = **다음 조개 구간을 넘기는 최저가** (단순히 "조금 더 비싼 조합"이 아님 — 구간 보너스 코인을 확보하는 안) / C = A보다 싸면서 코인 최대인 부족안.
8. 구매 한도: HOT 상품 = 매일 daily개 × 일수, 나머지 = stock. 패키지 = 매일 daily회(레전드만 3) × 일수.
9. 이벤트 일수: 남은 시간 "4d 21h" → 올림해서 5일.

## 2. 데이터 구조 (기존 유지)

`items.js` — 아이템 마스터(이벤트 무관, 누적):
```js
window.ITEMS = { pure_gold: { name:"순금", icon:"icons/pure_gold.png", qty:58 }, ... }
```
- **이번 작업에서 `qty`(기본 단위수량) 필드를 추가할 것.** 현재 이벤트 기준값: 순금58, 공용조각12, 망치75(HOT형)/30(일반형은 이벤트별로 다름→기본값은 HOT형 기준), 미스릴1, 레전드장비상자1, 공용섬조각1, 일반장비상자30, 설계스케치28, 금사140, 비단14000, 보석도면24, 보석매뉴얼24, 펫먹이4800, 펫돌파상자7, 고급훈련기록10.
- 같은 아이템이 상점에 여러 슬롯(HOT 순금 58개 / 일반 순금 15개)으로 나올 수 있다. 슬롯별 qty는 이벤트 데이터가 갖는다. 마스터 qty는 OCR 검증용 기본값일 뿐이다.

`event-YYYY-MM.js` — 이벤트 데이터(월별 생성):
```js
window.EVENT_DATA = { id, name, days,
  shop:[{item, qty, coin, type:"hot"|"rec"|"normal", daily?, stock?}],
  packages:[{id,name,price,coin,shell,daily}],
  freeDaily:{coin,shell}, dailyQuest:{coin,shell}, doublePack:{price},
  journey:{coin,shell}, shellTiers:[{shell,coin}] }
```

## 3. 이번 작업: `admin.html` (관리자 페이지)

목적: 운영자가 이벤트 스샷을 드래그하면 브라우저가 상품·숫자를 읽고, 확인/수정 후 "게시"하면 새 `event-YYYY-MM.js`가 리포에 커밋되어 Netlify가 자동 재배포된다.

### 3.1 접근 제어
- 진입 시 비밀번호 입력. 해시(SHA-256) 비교로 클라이언트에서 1차 차단하되, **실제 보안은 게시 API(서버리스 함수)의 비밀 검증**이 담당한다. 클라이언트 해시는 편의용임을 주석으로 명시.

### 3.2 인식 엔진 (브라우저, 순수 JS + Tesseract.js)
Python 프로토타입으로 원본 해상도(1080×2340) 스샷에서 검증 완료된 파라미터. JS 이식 시 동일하게 적용할 것.

**아이콘 매칭 (검증: 17/17 정확, 오탐 0)**
- 템플릿 = 마스터 아이콘의 내부 영역만: x 12%~88%, y 12%~68% (우하단 숫자·좌상단 뱃지 회피)
- 멀티스케일 매칭: scale 0.25~1.4를 24단계, 정규화 상관계수(TM_CCOEFF_NORMED 상당) ≥ 0.72
- NMS: 중심 거리가 템플릿 폭·높이의 0.6배 이내면 낮은 점수 제거
- 마스터에 없는 아이템(가속 등)은 자연히 매칭되지 않음 → 무시가 정상 동작
- JS 구현: canvas ImageData로 직접 구현하거나 opencv.js 사용(용량 크면 직접 구현 권장. 그레이스케일+정규화 상관으로 충분)

**숫자 OCR (Tesseract.js, digits whitelist, psm 7/8 병행, 3배 확대)**
매칭된 타일 기준 상대좌표(S = 아이콘 한 변 픽셀, (ix,iy) = 아이콘 좌상단):
- 잔여 텍스트: x -0.40S~1.40S, y 1.08S~1.40S — **흰 글자 마스크**(V>200, S<60) → 연결요소 중 높이 < 최대높이×0.55 제거(콜론·"잔여:" 잔재 제거) → 열 투영으로 단어 분리(간격 > 영역높이×0.22) → **마지막 단어만** OCR. 검증: 17/17
- 코인 가격: x -0.15S~1.35S, y 1.58S~1.94S — **흰 배경 반전 마스크**(255-흰마스크) → 마지막 단어(코인 아이콘과 분리됨). 검증: 15/17 (화면 최하단 잘림 2건 → 잘린 타일은 신뢰도 낮음 표시)
- 단위수량(아이콘 우하단): **OCR 불안정(12~14/17)** → 마스터 `qty` 기본값을 쓰고, OCR 결과가 기본값과 다를 때만 해당 칸을 노란색 강조하여 운영자가 확인. OCR 값을 자동 채택하지 말 것.
- "14K" 같은 K 접미사는 ×1000 처리. 콤마 제거.

**HOT/추천 구분**: 타일 좌상단 모서리 색으로 판정 — 주황 계열=HOT, 초록 계열=추천, 없으면 일반. (아이콘 좌상단 밖 -0.10S~0.10S 영역의 dominant hue)

**패키지·특훈·여정 스샷**: 동일 엔진으로 코인/조개 아이콘 위치의 숫자를 읽되, 구조가 단순하므로 실패 시 수동 입력 폼으로 충분. v1은 상점 스샷 자동화가 핵심이고 패키지·특훈·여정은 **기본값 프리필 + 수동 확인**으로 시작해도 된다(값이 이벤트마다 거의 안 변함).

### 3.3 확인 UI
- 인식 결과를 상점 표(아이콘·이름·qty·가격·잔여·구분)로 표시, 모든 칸 인라인 편집 가능
- 기본값과 다른 칸·신뢰도 낮은 칸은 노란색 강조
- 일수(days) 입력: 스샷의 카운트다운("4d 00:16")을 OCR 하기보다 운영자가 직접 입력(기본값 = 직전 이벤트와 동일)
- 미검출 상품 수동 추가: 마스터 아이콘 그리드에서 클릭해 행 추가

### 3.4 게시 (커밋) 플로우
- `netlify/functions/publish.js` (Netlify Function): body = {password, filename, content}
  - `process.env.ADMIN_PASSWORD` 검증 → GitHub Contents API로 `event-YYYY-MM.js` 커밋 (`process.env.GITHUB_TOKEN`, repo `jasonshin1hm/kingshot-kvk`, branch main)
  - 같은 파일이 있으면 sha 조회 후 업데이트
- `index.html`의 `<script src="event-*.js">`는 **고정 파일명 `event-current.js`로 변경**하고, 게시 시 `event-current.js`를 덮어쓰는 방식으로 단순화 (index.html 수정 커밋 불필요). 기존 `event-2026-08.js`는 이력 보존용으로 `events/` 폴더에 사본 커밋.
- 환경변수(ADMIN_PASSWORD, GITHUB_TOKEN)는 Netlify 대시보드에서 운영자가 직접 설정. 코드/문서에 토큰을 넣지 말 것.
- GitHub Pages 쪽 계산기도 같은 리포를 보므로 커밋만으로 양쪽 다 갱신된다.

### 3.5 완료 기준 (Claude Code가 스스로 검증할 것)
1. `admin.html`에 `/test-shots/`의 스샷을 넣었을 때 상점 17개 슬롯이 전부 올바른 아이템으로 표시된다 (test-shots는 운영자가 리포에 올려줌; 없으면 요청할 것)
2. 잔여·가격 인식률이 표에서 눈으로 확인 가능하고, 틀린 칸 수정 → 게시 → `event-current.js` 커밋이 동작한다
3. 기존 `index.html` 계산기의 A/B/C 결과가 이 시나리오에서 변하지 않는다: 순금 5일 + 미스릴 25 + 레전드장비상자 9 + 보상2배 → A = ₩202,500 (p1×5, p2×5, p3×5, p4×5, p5×2 + 2배)
4. kvk.html / data.js / t4.mjs 에 diff가 없다

## 4. 스타일
- 기존 index.html의 CSS 변수(크림/골드 팔레트)를 admin.html에도 재사용. 모바일 우선(공개 페이지), admin은 데스크톱 기준이어도 무방.
- 한국어 UI. 이모지 아이콘은 기존 페이지 수준으로만.
